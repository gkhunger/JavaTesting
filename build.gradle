apply plugin: 'java'

dependencies {
    testCompile 'junit:junit:4.11'
}

repositories {
  jcenter()
}

sourceSets {
     main {
        java {
            srcDir 'src'
        }
    }
    test {
        java {
            srcDirs = ["test"]
        }
    }
}
